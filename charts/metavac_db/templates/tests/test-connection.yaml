apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metavac-db.fullname" . }}-test-connection"
  labels:
    {{- include "metavac-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metavac-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
